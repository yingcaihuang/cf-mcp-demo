{
  "name": "racore-cdn-mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Remote MCP Server on Cloudflare Workers exposing Racore CDN Statistic Analysis APIs",
  "keywords": [
    "mcp",
    "model-context-protocol",
    "cloudflare-workers",
    "cdn",
    "racore"
  ],
  "main": "src/index.ts",
  "engines": {
    "node": ">=22.0.0"
  },
  "cloudflare": {
    "bindings": {
      "RACORE_ACCESS_KEY": {
        "description": "Racore 控制台的 **access_key**。与 secret_key 一同用于换取 24 小时有效的 API token。"
      },
      "RACORE_SECRET_KEY": {
        "description": "Racore 控制台的 **secret_key**。仅用于在 Worker 内部计算 HMAC-SHA512 签名，不会返回给 MCP 客户端。"
      },
      "RACORE_API_BASE_URL": {
        "description": "Racore OpenAPI 的网关地址，保持默认的 `https://portal.racorecloud.com` 即可。注意 `api.racorecloud.com` 的 HTTP 层不响应，填成它会导致鉴权连接超时。"
      }
    }
  },
  "scripts": {
    "postinstall": "wrangler types",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "wrangler types && tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "agents": "^0.24.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "wrangler": "^4.135.0"
  }
}
